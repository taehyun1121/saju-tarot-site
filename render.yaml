# Render 무료 배포 설정 (고삼타로 백엔드) — Railway 대체(체험 만료)
# Render 대시보드에서 이 레포 연결(Blueprint) 시 자동 인식.
services:
  - type: web
    name: gosamtarot-backend
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: CORS_ORIGINS
        value: "https://gosamtarot.com,https://www.gosamtarot.com,https://taehyun1121.github.io,https://talddung.com,http://localhost:5173"
